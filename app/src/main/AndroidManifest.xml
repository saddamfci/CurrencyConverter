<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roxy.mind_reader.currencyconverter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".USDActivity" />
        <activity android:name=".EURActivity" />
        <activity android:name=".INRActivity" />
        <activity android:name=".PKRActivity" />
        <activity android:name=".QARActivity" />
        <activity android:name=".SARActivity" />
        <activity android:name=".MYRActivity" />
        <activity android:name=".JODActivity"></activity>
    </application>

</manifest>